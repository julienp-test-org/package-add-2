name: package-add-2
description: A minimal Python Pulumi program
runtime:
  name: python
  options:
    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: python
packages:
  random:
    source: terraform-provider
    version: 1.0.1
    parameters:
      - opentofu/random
